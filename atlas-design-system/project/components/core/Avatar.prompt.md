Circular user avatar with optional gold level badge — the profile identity element.

```jsx
<Avatar name="Maya Okonkwo" level={24} ring src="/me.jpg" />
<Avatar name="Tom Reilly" size={32} />
```

Falls back to initials when `src` is absent. `level` shows a gold corner badge; `ring` adds the gold outline used for the signed-in user.
